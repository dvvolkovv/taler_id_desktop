import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Smoke test placeholder — full tests require DI and Firebase setup
    expect(true, isTrue);
  });
}
